# Uptime Kuma — public status page (ADR-008).
resources:
  requests:
    cpu: 25m
    memory: 128Mi
  limits:
    cpu: 300m
    memory: 256Mi

persistence:
  enabled: true
  size: 2Gi
  storageClass: local-path
