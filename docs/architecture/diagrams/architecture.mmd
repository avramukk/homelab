flowchart TB
  subgraph internet [Internet]
    visitor[Visitor]
  end

  subgraph cf [Cloudflare]
    edge[Edge / TLS]
  end

  subgraph host [lab-host · Apple Silicon M4 · 24 GiB]
    subgraph k3s [k3s cluster · k3d · 1 server + 2 agents]
      traefik[Traefik ingress]
      argo[Argo CD]
      obs[Prometheus · Loki · Grafana · Tempo]
      kuma[Uptime Kuma]
      sealed[Sealed Secrets]
      cfd[cloudflared]
    end
    restic[(Restic repo · local disk)]
  end

  gh[GitHub · public repo]
  tg[Telegram]
  op[Operator]

  %% Public: status page only
  visitor -->|HTTPS| edge
  edge -->|tunnel| cfd
  cfd --> kuma

  %% Private: operator over Tailscale
  op -->|Tailscale| traefik
  traefik --> obs

  argo -->|pull| gh
  obs --> restic
  obs -->|alerts| tg
