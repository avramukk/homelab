# macOS
.DS_Store

# Secrets — never commit
*.credentials*
.credentials*
**/secrets/
*.pem
*.key
.env
.env.*

# Terraform / OpenTofu
*.tfstate
*.tfstate.*
.terraform/
*.tfvars
!*.tfvars.example

# Helm / kubeconfig
kubeconfig*
*.kubeconfig

# Editors / OS
*.swp
*.swo
.idea/
.vscode/
*.tmp

# Local task tracking (never committed)
TODO-*.md

# Personal backlog/scratchpad (kept local, not part of the public repo)
INBOX.md

# Tooling artifacts
.playwright-mcp/

# Agent project skills are part of the showcase.
# Re-include here because a global ignore (~/.gitignore_global) excludes **/.opencode/.
!.opencode/
!.opencode/**